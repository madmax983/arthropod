[package]
name = "input-fusion"
version = "0.1.0"
edition = "2021"
authors = ["Arthropod Contributors"]
license = "MIT OR Apache-2.0"
description = "Gesture recognition and input sequencing for Arthropod"

[dependencies]
flux-state = { path = "../../crates/flux-state" }
plat-core = { path = "../../crates/plat-core" }
log = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
flux-state = { path = "../../crates/flux-state" }
plat-core = { path = "../../crates/plat-core" }
