[package]
name = "anim-graph"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Animation system for Arthropod GUI framework"

[dependencies]
render-engine = { path = "../render-engine" }
log.workspace = true
