[package]
name = "input-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Input and gesture recognition engine for Arthropod GUI framework"

[dependencies]
flux-state = { path = "../flux-state" }
plat-core = { path = "../plat-core" }
indexmap = "2.13.0"
serde = { version = "1.0", features = ["derive"] }

[features]
nova = []

[dev-dependencies]
flux-state = { path = "../flux-state" }
plat-core = { path = "../plat-core" }
criterion = "0.5"
proptest = "1.11.0"

[[bench]]
name = "form_bench"
harness = false
