[package]
name = "layout-engine"
version = "0.1.0"
edition = "2021"
authors = ["Arthropod Contributors"]
license = "MIT OR Apache-2.0"
description = "Flexbox layout engine for Arthropod GUI framework"

[dependencies]
taffy = "0.6"
glam = "0.29"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "layout_bench"
harness = false
